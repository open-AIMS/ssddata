% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ssddata.R
\name{ssd_data_sets}
\alias{ssd_data_sets}
\title{List SSD Datasets}
\usage{
ssd_data_sets(set = "v2", group = NULL, dedup = "geomean", cas_lookup = TRUE)
}
\arguments{
\item{set}{A string or character vector controlling which datasets are
returned. Options:
\itemize{
\item \code{"v2"} (default): all current individual non-aggregate datasets
\item \code{"v1"}: fixed set of 20 datasets from ssddata v1
\item one or more prefix strings e.g. \code{c("ccme", "anzg")}: filters v2 by prefix
\item \code{"wqbench"}: splits \code{wqbench_data} by \code{Chemical}
\item \code{"envirotox_acute"}: splits \code{envirotox_acute} by \code{Chemical}
\item \code{"envirotox_chronic"}: splits \code{envirotox_chronic} by \code{Chemical}
\item \code{"anztox"}: splits \code{anztox_data} by \code{chemicalname_grouped} x \code{mediatype}
\item \code{"alldata"}: aggregates all \verb{*_data} sources and splits by \code{Chemical}
}}

\item{group}{A character vector of column names to further split datasets by
before returning. Columns absent from a dataset are silently skipped.
Column values are always appended to dataset names when the column is
present. Default \code{NULL} applies no additional splitting.}

\item{dedup}{A string controlling how duplicate Species rows within a
chemical are handled. \code{"geomean"} (default) applies a geometric mean and
emits a message; \code{"none"} returns data as-is and emits a message listing
duplicates.}

\item{cas_lookup}{A flag. When \code{TRUE} (default) and \code{set = "alldata"},
uses \code{data-raw/anztox/cas_parent_lookup.csv} to align chemical names/CAS
numbers across datasets before splitting.}
}
\value{
A named list of tibbles.
}
\description{
Returns a named list of SSD datasets from ssddata.
}
\examples{
ssd_data_sets()
ssd_data_sets(set = "v1")
ssd_data_sets(set = c("ccme", "anzg"))
}
